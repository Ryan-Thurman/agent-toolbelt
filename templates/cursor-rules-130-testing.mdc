---
description: Dev testing rules
globs: "**/*.test.*,**/*.spec.*,**/test/**,**/tests/**,**/__tests__/**"
alwaysApply: false
---

Behavior changes need tests or an explicit documented reason tests are not
added.

Map tests to acceptance criteria when feature metadata exists.

Do not claim tests passed unless they were run and the result is known.
